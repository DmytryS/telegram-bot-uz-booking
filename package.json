{
  "name": "telegram-uz-booking-bot",
  "version": "0.0.1",
  "repository": {
    "type": "git",
    "url": "git@github.com:DmytryS/uz-booking-bot.git"
  },
  "scripts": {
    "lint": "lerna run lint",
    "dev": "docker-compose up",
    "bootstrap": "lerna bootstrap",
    "postversion": "git push && git push --tags"
  },
  "devDependencies": {
    "lerna": "^3.22.0"
  }
}
